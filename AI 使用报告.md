# AI 使用报告

> 本报告按《全国大学生数学建模竞赛论文格式规范》第十条与赛前培训的要求编写，
> 随支撑材料一并提交。报告内不含参赛队、学校与赛区信息。

## 一、声明

本参赛队在 2026 年竞赛期间**使用了 AI 工具**（大语言模型编程助手，
主要用于代码编写与调试）。论文正文已按规范在**参考文献之前**作出声明。

## 二、使用范围

| 环节 | 是否使用 AI | 具体用途 |
| --- | --- | --- |
| 问题理解与模型选型 | 部分 | 用于梳理题面给定的物性经验式与边界条件，讨论"水分方程该用经典 Fick 还是把体积密度写回"这一类闭合问题，并检索相关文献线索 |
| 公式推导 | 部分 | 用于把推导过程写成可核对的形式（体积密度与干基密度的恒等式、贴体坐标下对流项与坐标变换项的抵消）；**每一步都用手算与符号代入复核** |
| 程序编写与调试 | 是 | 数值求解器、对照实验、结果表生成、插图脚本的编写与排错，由 AI 辅助起草，参赛队逐行审阅、修改并运行 |
| 数据处理与核对 | 是 | 自动生成 347 项"论文数字 vs 复算值"的核对脚本（`代码/verify_paper_numbers.py`），以及守恒检验、网格/时间步收敛、交叉验证等自检程序 |
| 插图绘制 | 是 | 绘图脚本与版面自检（缺字/裁切/刻度重叠）由 AI 辅助编写 |
| 论文写作与排版 | 是 | 语言润色、LaTeX 排版与格式规范自查 |

## 三、人工把关方式

凡由 AI 参与产生的结论与数字，都经过下列**可复现的独立检验**（全部随包提供）：

1. **守恒检验**：问题一水分守恒残差 $6.8\times10^{-14}$（机器精度量级）；
2. **交叉验证**：把问题二求解器的物性换成附录 2 的常数物性后，与问题一的
   独立求解器逐点比对，温度场最大偏差 $2.6\times10^{-12}$ °C；
3. **网格与时间步收敛**：$N=20\sim400$、$\Delta t=2\sim120$ s 共 20 组对照，
   烘干时长的增量随网格加密逐次减半（一阶收敛）；
4. **数据核对**：论文表 1–表 6 与 `result1–4.xlsx` 逐格比对零差异；
   全文 347 项可核验数字全部重算一致（见 `数据核对报告.md`）；
5. **口径对照**：三种水分方程口径分别独立实现并对同一算例复算，
   结果互相印证（`代码/p23_variants.py`、`代码/closure_rhod.py`）；
6. **规范自查**：论文页数、页边距、页码位置、身份信息、PDF 体积逐项脚本化检查。

## 四、结论责任

模型假设、方程口径的取舍、参数取值与全部结论均由参赛队独立判断并逐项复核；
AI 仅作为编程与文字处理的辅助工具。论文与支撑材料中的全部数值结果都由本地
运行上述程序产生，没有直接引用 AI 给出的任何数值结论。

## 五、详细日志

完整的生成与核对过程记录见支撑材料：

- `数据核对报告.md`：347 项数据核对结果与发现的问题清单；
- `代码/代码结构.md`：各程序的功能、依赖关系与运行顺序；
- `README.md`：环境与一键复现步骤（`setup.sh` + `reproduce.sh`）。
