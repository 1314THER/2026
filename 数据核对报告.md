# 论文数据核对报告

> 核对对象：`论文/论文全文.tex` 与 `输出/result1–4.xlsx`、`输出/*.npz`、
> `输出/figdata/*`、`输出/问题三-网格与口径对照.csv`。
> 复核脚本：`代码/verify_paper_numbers.py`（347 项）、`代码/closure_rhod.py`（第三种口径）。
> 本报告只记录核对结果与建议改法，**不修改论文正文**。

---

## 一、结论摘要

1. 全文 **347 项数据核对全部一致**，其中论文表 1–表 6 与 `result1–4.xlsx`
   的 **234 个单元格逐格比对零差异**；
2. 表 14 的第三种口径（干基密度守恒）此前**没有实现、数字无法复现**，
   本次补算得到 **48.39 h**（N=100），论文原写的 **51.39 h 有误**；
3. 同一处把该口径说成"等效于把扩散系数放大到约 $(1+C)D$"也**不成立**，
   正确倍数是 $\rho_d/(\rho_d+C\rho_d')$，在 $C_0$ 处约 **1.62 倍**；
4. 本轮之前已发现并修正的 12 处数据问题见第四节；
5. 论文中已无未定义引用（`??` 出现 0 次）。

---

## 二、第三种口径：干基密度守恒（本次新增核算）

### 2.1 口径定义

问题三的几何固定（$R\equiv2$\,cm），温度方程不变，三种口径只在水分方程
的"累积项与通量项用什么密度"上不同：

| 口径 | 方程 | 累积系数 | 通量系数 |
|---|---|---|---|
| 经典 Fick（本文） | $\partial_tC=\nabla\!\cdot(D\nabla C)$ | $1$ | $D$ |
| 体积密度守恒 | $\partial_t(\rho C)=\nabla\!\cdot(\rho D\nabla C)$ | $\rho+C\rho'$ | $\rho D$ |
| **干基密度守恒** | $\partial_t(\rho_dC)=\nabla\!\cdot(\rho_dD\nabla C)$ | $\rho_d+C\rho_d'$ | $\rho_dD$ |

其中 $\rho=a+bC$（附录 3，$a=650,\ b=128$），$\rho_d=\rho/(1+C)$，
$\rho_d'=\mathrm{d}\rho_d/\mathrm{d}C=(b-a)/(1+C)^2$。

### 2.2 计算结果

固定半径 $R_0$、附录 3 物性、$\Delta t=5$\,s：

| 口径 | $N=100$ | $N=200$ | 相对 Fick（N=100） |
|---|---|---|---|
| 经典 Fick | 206365 s = **57.32 h** | 206700 s = **57.42 h** | — |
| 体积密度守恒 | 219340 s = **60.93 h** | 219685 s = **61.02 h** | $+6.3\%$ |
| **干基密度守恒** | 174195 s = **48.39 h** | 174490 s = **48.47 h** | $\mathbf{-15.6\%}$ |

三种口径折合天数分别为 **2.39 / 2.54 / 2.02 天**，均落在题面所述
"$2\sim3$ 天"区间内，定性结论不变。

### 2.3 可信度：与主求解器的交叉验证

`代码/closure_rhod.py` 直接复用 `solve_p234.py` 的物性经验式、空气条件、
三对角组装与追赶法，只替换累积系数与通量系数。它给出的

- 经典 Fick：N=100 得 206365 s、N=200 得 206700 s；
- 体积密度守恒：N=100 得 219340 s、N=200 得 219685 s；

与 `输出/问题三-网格与口径对照.csv` **逐位一致**，说明驱动方式与主对照
求解器同源，第三种口径的数字同样可信。

网格收敛性也一致：$N=100\to200$ 时第三种口径由 48.39 h 变到 48.47 h，
相对变化 $0.17\%$，与另两种口径同量级。

### 2.4 论文中需要修正的 6 处

定位方式用**原文片段**而非行号（论文仍在编辑，行号会变）：在 `论文全文.tex`
中搜索 `51.39`、`10.4\%`、`(1+C)D` 三个串即可命中全部 6 处。

| 出现位置（原文片段） | 现在的写法 | 建议改为 |
|---|---|---|
| "…$\rho_d=\rho/(1+C)$ 写守恒形式，等效于把扩散系数放大到约 $(1+C)D$。" | $(1+C)D$ | 放大约 $1.1\sim1.6$ 倍 |
| 表 14 第三行"变密度守恒口径（等效 $D\to(1+C)D$）" | `51.39`、`-10.4%` | **`48.39`**、**`-15.6%`** |
| "…按干基密度 $\rho_d$ 的守恒形式则缩短 $10.4\%$。" | $-10.4\%$ | $-15.6\%$ |
| 误差来源表二"…烘干时长分别变化 $+6.3\%$ 与 $-10.4\%$（表 14）" | $-10.4\%$ | $-15.6\%$ |
| "…干基密度守恒口径差 $-10.4\%$。" | $-10.4\%$ | $-15.6\%$ |
| "…方程口径的选择带来 $+6.3\%\sim-10.4\%$ 的偏移。" | $-10.4\%$ | $-15.6\%$ |

### 2.5 为什么不是"$(1+C)D$"

把守恒形式展开：

$$
\frac{\partial(\rho_dC)}{\partial t}=\nabla\!\cdot\!(\rho_dD\nabla C)
\ \Longrightarrow\
\frac{\partial C}{\partial t}
=\frac{1}{\rho_d+C\rho_d'}\nabla\!\cdot\!(\rho_dD\nabla C)
\ \approx\
\frac{\rho_d}{\rho_d+C\rho_d'}\,\nabla\!\cdot\!(D\nabla C).
$$

即等效放大倍数 $k=\rho_d/(\rho_d+C\rho_d')$。由于 $\rho_d'<0$，分母小于
$\rho_d$、$k>1$，扩散被放大；但 $k$ 随含水率下降迅速回落到 1：

| $C$ | 2.55 | 1.00 | 0.50 | 0.15 |
|---|---|---|---|---|
| $k$ | 1.62 | 1.51 | 1.35 | 1.11 |

所以正确说法是"**放大约 $1.1\sim1.6$ 倍**"。原写法 $(1+C)D$ 在 $C_0$ 处为
$3.55D$，与实测的 $1.62D$ 相差一倍以上，也解释不了为什么该口径只让烘干
时长缩短 $15.6\%$ 而非数倍。

---

## 三、全文数据审计（347 项）

审计脚本：`代码/verify_paper_numbers.py`。全部通过，覆盖范围如下。

| 类别 | 核对内容 | 结果 |
|---|---|---|
| 论文表 1–表 6 | 与 `result1–4.xlsx` 逐格比对，共 **234 格** | 零差异 |
| 表 12、表 13（网格 / 时间步） | 与 `输出/问题三-网格与口径对照.csv` | 逐位一致 |
| 表 11（参数灵敏度） | 与 `输出/figdata/sens.csv` | 逐位一致 |
| 表 14 前两行（口径） | 同上 CSV | 逐位一致 |
| 表 13（干物质守恒） | 由 `result23/4_data.npz` 重算 | 一致 |
| §11 判据与 $b$ 扫描（31 项） | 与 `输出/收缩判据与代价.json` | 一致 |
| 表 2（噪声诊断） | 由附件 1 重算（高斯加权局部二次趋势，$w=15$） | 一致 |
| 表 5 / 表 6（问题一网格与插值） | 由 `solve_p1` 重算 | 一致 |
| 结果文件规格 | 由 `result_meta.json` 与 xlsx 实测行数 | 一致 |

复现方式：

```bash
PY=/Users/sqz/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3
$PY 代码/verify_paper_numbers.py            # 全量审计，打印不一致项
$PY 代码/closure_rhod.py --all              # 三种口径对照 + 交叉验证
```

---

## 四、本次核对中已修正的数据问题（12 处）

以下问题均已修正到 `论文全文.tex` 中。

| 位置 | 原来的数字 | 实际值 | 性质 |
|---|---|---|---|
| §5.6 表（$\beta$ 反演） | 附录 3 端点 $\beta=0.823$ | **0.822** | 末位错误 |
| §5.6 表题 | $T_\infty$ 末 1 h 均值 49.9957 °C | **49.9989 °C** | 与问题二自相矛盾 |
| §5.6 表题 | 湿度范围 0.0498~0.0501 | **0.0498~0.0502** | 上界偏低 |
| 表 13（干物质守恒） | 问题四 $-25/-13/-26\%$ | **$-27.0/-13.8/-10.9\%$** | 末值为旧结果 |
| 同表图注 | "稳定在 $-7\%$ 附近" | 已按实算改写 | 与表自相矛盾 |
| 表 5（平滑方式） | 5 点 / 15 点 SG 两行数字 | 按可复现口径重算 | 无法复现 |
| 表 6（插值方式） | 样条过冲 0.032 °C | **0.080 °C** | 无法复现 |
| §5.2 正文 | "最多 0.2% 格子"、"0.002~0.027 °C" | **0.4%**、**0.014~0.055 °C** | 与更新后的表不符 |
| 图 d13 图注 | "$D$ 下降约两个数量级" | **约 7 倍**（同温 17 倍） | 量级夸大 |
| 图 d15 图注 | 表面 14.2 h 达标（23%） | **12.6 h（22%）** | 与表 tab:radial 矛盾 |
| 图 d03 图注 | 峰值 $-0.22$ cm/h @4.3 h；50% 收缩 2.4 h | **$-0.27$ cm/h、2.5 h** | 位置与数值都不对 |
| §10 与误差表 | 口径差 12974~12960、波动 14 s；$\Delta t=60$ s 差 200 s；$\Delta t=120$ vs 2 s 差 392 s | **12960~12980、20 s；215 s；406 s** | 与对照 CSV 不符 |

此外修正了一处公式错误：假设 (A2) 中端面占比原写作 $2R/L=8\%$
（按此算得 $16\%$），应为 $R/L=2/25=8\%$。

---

## 五、口径的完整对照（供表 14 使用）

三种口径的问题三烘干时长（固定半径 $R_0$、附录 3 物性、$\Delta t=5$\,s）：

| 水分方程口径 | 烘干时长（$N=100$） | 相对基准 | 烘干时长（$N=200$） |
|---|---|---|---|
| 经典 Fick：$\partial_tC=\nabla\!\cdot(D\nabla C)$ | 57.32 h | — | 57.42 h |
| 体积密度守恒：$\partial_t(\rho C)=\nabla\!\cdot(\rho D\nabla C)$ | 60.93 h | $+6.3\%$ | 61.02 h |
| 干基密度守恒：$\partial_t(\rho_dC)=\nabla\!\cdot(\rho_dD\nabla C)$ | **48.39 h** | $\mathbf{-15.6\%}$ | **48.47 h** |

三种口径的极差为 57.32 → 48.39 h，即 $-15.6\%$，仍小于 $D$ 的
$\pm20\%$ 灵敏度（48.9~70.0 h），大于网格与时间步引入的数值误差
（$\le1.6\%$ 与 $\le0.2\%$），因此论文"口径差大于数值误差、小于参数
不确定度"的论断依然成立。

---

## 六、仍未处理的事项

1. 第二节列出的 **6 处正文改动**尚未落到 `tex`（按要求未改）；
2. 论文正文现约 49 页，距 30 页目标还差约 19 页，主要分布在
   §7 问题二（8 页）、§10 模型的检验与灵敏度（8 页）、§13 模型的评价与推广（7 页）；
3. 论文中已无未定义引用，删图删表留下的失效引用已在早前一并修掉。
