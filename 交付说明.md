# 交付说明（提交清单 + 格式自查）

> 对照《全国大学生数学建模竞赛论文格式规范（2026 年修订稿）》与赛前培训
> 要求逐条自查，实测数据由脚本给出，可重跑复核。

---

## 一、要提交的两个文件

| 提交物 | 本包中的文件 | 提交前要做的 |
| --- | --- | --- |
| 电子版论文（必须是 PDF，不压缩） | `论文/论文全文_交付版.pdf` | 重命名为 **校内编号.pdf** |
| 电子版支撑材料（ZIP/RAR） | `支撑材料.zip`（用 `./打包支撑材料.sh` 生成） | 重命名为 **校内编号.zip** |

文件名中不要出现姓名、学校、赛区（校内编号由学校/赛区下发）；
电子版论文里也**不要**出现承诺书与编号专用页——本论文第一页即为摘要页，
符合"电子版论文第一页必须为摘要专用页"的要求。

---

## 二、格式自查（逐条对照规范）

| 规范要求 | 实测 | 结论 |
| --- | --- | --- |
| 摘要页原则上不超过一页 | 摘要 1 页（含标题与关键词） | ✅ |
| 正文不超过 30 页、不要目录 | 正文 29 页（第 2–30 页），参考文献自第 31 页起（与附录 A 同页） | ✅ |
| 附录页数不限，须含支撑材料文件列表与全部源程序 | 附录第 31–83 页；核心源程序 11 个全文列出，其余 8 个（出图、写表、环境自检、交付 PDF）在附录中声明"全文随支撑材料提供" | ✅ |
| 上下左右各留出至少 2.5 cm 页边距 | 逐页栅格化实测最小值：左 2.71 / 右 2.67 / 上 3.68 / 下 2.50 cm | ✅ |
| 页码位于页脚中部，从"1"开始连续编号 | 页码在页脚居中，第 1 页即摘要页 | ✅ |
| 摘要、正文、附录中不得出现学校、姓名、赛区信息 | 全文检索 0 处；附录代码中的本机路径也已清除 | ✅ |
| 使用 AI 工具须在参考文献之前声明，并在支撑材料中提供详细报告 | 论文正文末（参考文献之前）有声明；`AI 使用报告.md` 随支撑材料提交 | ✅ |
| 电子版为单个 PDF、不压缩、不超过 20 MB | 单个 PDF，1.4 MB | ✅ |
| 电子版第一页为摘要专用页 | 第 1 页为标题 + 摘要 + 关键词 | ✅ |

自检命令（可重跑）：

```bash
python3 代码/make_delivery_pdf.py 论文/论文全文.pdf 论文/论文全文_交付版.pdf
# 输出里会打印缩放比例、页数、页面尺寸与四边最小页边距
```

---

## 三、页边距是怎么合上的（不改排版、不截图）

论文排版本身是按 `\geometry{left=2.2cm,right=2.2cm,top=2.6cm,bottom=2.4cm}`
做的，直接打印不满足"至少 2.5 cm"。把 geometry 改成 2.5 cm 会让每行变短、
全文重排、页数上涨（正文已经用到接近上限的 29 页），而且很可能把正文推过 30 页。

因此这里采用**整体等比缩放**：`代码/make_delivery_pdf.py` 把编译出的每一页按
同一比例（实测 0.9078）缩小后居中放到 A4 上——

* 分页完全不变（正文仍 29 页，总 83 页）；
* 没有任何内容被裁切，文字仍是可选可检索的文本、插图为矢量图；
* 四边页边距都拉开到 2.5 cm 以上（见上表）。

`论文/论文全文.pdf` 是**未缩放**的编译原版，供继续修改；改动后重跑上面那条
命令即可再生成交付版。

---

## 四、支撑材料的内容与体积

用 `./打包支撑材料.sh` 生成 `支撑材料.zip`（默认排除可以由脚本重算的大文件）：

| 目录/文件 | 内容 | 体积 |
| --- | --- | --- |
| `代码/` | 全部可运行源程序（求解、对照、出表、出图、自检）与 `代码结构.md` | ≈0.5 MB |
| `输出/` | `result1/3/4.xlsx`、正文各表、对照 CSV、审计 JSON、`figdata/`、`figs/*.pdf` | ≈9 MB |
| `论文/` | `论文全文.tex` 与所需插图源文件（PDF 交付版单独提交，不打进包） | ≈0.6 MB |
| `建模/`、`补充分析/` | 建模推导、收缩判据的独立复核脚本与报告 | ≈0.3 MB |
| 题面、附件、模板 | `A题-*.md/.pdf`、附件 1/2、附件 3 模板 | ≈0.6 MB |
| 说明文档 | `README.md`、`交付说明.md`、`AI 使用报告.md`、`数据核对报告.md` | ≈0.05 MB |
| **合计（不含 result2.xlsx）** | | **≈11 MB** ✅ 低于 20 MB |

> ⚠️ **`result2.xlsx` 单文件 27 MB**（206700 行 × 22 列 × 2 张表，约 910 万格，
> 1 s 间隔覆盖整个烘干过程）。虽然 xlsx 内部已是压缩格式，但整包再压一遍仍有
> 约 2.6 倍空间：实测 `./打包支撑材料.sh --with-result2` 得到 **16.4 MB**，**没有超过 20 MB**。
> 三种处理方式，按提交系统的实际情况选一种：
>
> 1. 系统有独立的结果文件上传项 → 把 `result1–4.xlsx` 单独上传（推荐）；
> 2. 只能交两个文件 → 用 `./打包支撑材料.sh --with-result2` 生成包含
>    `result2.xlsx` 的大包（实测 16.4 MB，**仍在 20 MB 以内**，
>    脚本会自动给出体积自检结论）；
> 3. 只交小的结果表 → 默认包里已含 `result1/3/4.xlsx`，`result2.xlsx` 由
>    `./reproduce.sh --fast` 一键重算（约 8 分钟）。

---

## 五、提交前最后三步

1. 跑一次 `./setup.sh` 与 `./reproduce.sh --fast`，确认四个 `result*.xlsx` 与
   论文附录里的表一致（这一步也可由他人复核）；
2. 用 `./打包支撑材料.sh` 生成支撑材料，并按校内编号重命名两个文件；
3. 按时在竞赛系统上传（论文 PDF 与支撑材料压缩包），保留 MD5 码。

若赛区或学校对封面、字号、页数另有要求，以赛区/学校要求为准。
