# 出包时实测通过的一套精确版本（Python 3.9.6，macOS arm64）。
#
# 用这套版本完整跑过 ./reproduce.sh：四个 result 文件、全部对照实验、
# 派生数据与 40 张插图都成功生成，且四个 xlsx 与论文表 1-表 6 逐格一致、
# 求解中间产物（result*.bin、*_data.npz）与原结果逐位相同。
#
# 想完全复刻：
#   python3 -m venv .venv
#   ./.venv/bin/pip install -r requirements-lock.txt
#
# 直接装 requirements.txt（只给版本下限）在 Python 3.12 上同样验证通过；
# 两者只有 matplotlib/seaborn 的像素级外观差异，不影响任何数值结果。
numpy==2.0.2
openpyxl==3.1.5
matplotlib==3.9.4
seaborn==0.13.2
pandas==2.3.3
contourpy==1.3.0
cycler==0.12.1
et-xmlfile==2.0.0
fonttools==4.60.2
kiwisolver==1.4.7
packaging==26.3
pillow==11.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
